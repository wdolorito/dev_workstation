#!/bin/sh

#
# ~/local/bin/hibernate
#

CMD=""
LOCK="lock_screen"
LOGINCTL="$(which loginctl 2>/dev/null)"

if [ -z "$LOGINCTL" ]
then
	exit 127
fi

CMD="$LOGINCTL hibernate"

if ! $CMD
then
	printf "\nunable to run \"%s\"\n\n" "$CMD"
	exit 127
fi

$LOCK
