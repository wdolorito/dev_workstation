#!/bin/sh

#
# ~/local/bin/shutdown
#

CMD=""
LOGINCTL="$(which loginctl 2>/dev/null)"

if [ -z "$LOGINCTL" ]
then
	exit 127
fi

CMD="$LOGINCTL poweroff"

if ! $CMD
then
	printf "\nunable to run \"%s\"\n\n" "$CMD"
	exit 127
fi
