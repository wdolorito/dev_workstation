#!/bin/sh

#
# ~/local/bin/dhclient
#

DHCLIENT="$(which /usr/sbin/dhclient 2>/dev/null)"
DOAS="$(which doas 2>/dev/null)"

if [ -z "$DOAS" ]
then
	exit 1
fi

if [ -z "$DHCLIENT" ]
then
	exit 127
fi

$DOAS "$DHCLIENT" "$@"
