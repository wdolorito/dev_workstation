#!/bin/sh

#
# ~/local/bin/hybrid-sleep
#

CMD="hybrid-sleep"
LOCK="$(which lock_screen 2>/dev/null)"
LOGINCTL="$(which loginctl 2>/dev/null)"

if [ -n "$LOGINCTL" ]
then
	$LOGINCTL "$CMD"
	$LOCK 2>/dev/null
else
	echo "loginctl command not available."
	exit 127
fi