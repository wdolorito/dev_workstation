#!/bin/sh

#
# ~/local/bin/reboot
#

CMD=""
LOGINCTL="$(which loginctl 2>/dev/null)"

if [ -z "$LOGINCTL" ]
then
	exit 127
fi

CMD="$LOGINCTL reboot"

if ! $CMD
then
	printf "\nunable to run \"%s\"\n\n" "$CMD"
	exit 127
fi
