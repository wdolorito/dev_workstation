#!/bin/sh

#
# ~/local/bin/shutdown
#

CMD="poweroff"
LOGINCTL="$(which systemctl 2>/dev/null)"

if [ -n "$LOGINCTL" ]
then
	$LOGINCTL "$CMD"
else
	echo "loginctl command not available."
	exit 127
fi
