#!/bin/sh

#
# ~/local/bin/imv
#

case "$XDG_SESSION_TYPE" in
    "wayland" )
        imv-wayland "$@"
        ;;
    "x11" )
        imv-x11 "$@"
        ;;
    * )
        echo "Not in a graphical session."
        exit 1
        ;;
esac
